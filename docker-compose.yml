version: "3"
networks:
  lavatech:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 111.112.113.0/24


servives:

lavatech:
  image: centos
  stdin_open: true
  tty: true
  command: bin/bash
  
